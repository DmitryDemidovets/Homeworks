
Создать текстовый файл и ответить  на вопросы:
1) Что такое переменная
Переменная это структура данных, предназначенная для хранения данных, промежуточного, или конечного результата работы кода.

2) Что такое git push
Это команда для выгрузки содержимого локального репозитория в удаленный.

3) Git merge и rebase (что делают, в чем отличие)
Обе команды предназначены для интеграции изменений из одной ветки в другую.
Git merge - способ слияния, при котором существующие ветки никак не изменяются. 
Команда создает в ветке feature новый коммит, связывающий истории обеих веток.
Git rebase -способ перебазирования. Команда перебазирет функциональную ветку feature на главную ветку main.
Если вместо команды merge при коммитах используется rebase, 
эта команда перезаписывает историю проекта, и создает новые коммиты для каждого коммита в исходной ветке.
Перебазирование перезаписывает историю, а слияние ее сохраняет.
Главное преимущество rebase — более чистая история проекта.

4) Как запросить у пользователя ввод
С помощью команды input()

5) Что такое аннотация типов
Аннотация типов это специальный синтаксис, который позволяет определять тип переменной.
Служит подсказкой для разработчика.

6) Что такое инкапсуляция
Инкапсуляция это один из принципов ООП.
Служит для ограничения доступа к составляющим объекта-методам, переменным.

7) Что такое полиморфизм
Это принцип ООП, который заключается в использовании единственной сущности, 
к примеру, метод/оператор,  для представления различных типов в различных случаях использования.

8) Что такое CRUD
Это 4 операции над сущностями:
Create — создание
Read — чтение
Update — обновление
Delete — удаление

9) Что такое json
Это формат текстовых данных, который используется для обмена данными в веб приложениях

10) Каким запросом я могу достать все данные из таблицы
SELECT

11) Что такое многие-ко-многим
Это вид связи таблиц в базе данных.
Предполагает возможность связи одного или нескольких элементов из одной таблицы
с одним или несколькими элементами из другой таблицы.
Примером могут служить таблицы "клиенты" и "продукция", каждый из списка клиентов может совершить покупки из разной продукции.


12)  Что такое JOIN и какой он может быть.
JOIN — это команда в языке запросов SQL, необходимая для работы с базами данных.
Объединяет данные из двух разных таблиц в базе. 
Цель использования команды — получить нужное подмножество данных. 
Основные виды:

INNER JOIN
Оператор внутреннего соединения
Соединяет две таблицы.
Если дано две таблицы, A и B, захватывает ту часть данных, которая является общей для двух таблиц А и В.

LEFT [ OUTER ] JOIN
Оператор левого внешнего соединения. 
Оператор выполняет соединение левой и правой таблиц по условию.
Если дано две таблицы, A и B, захватывает всю левую часть (А), и часть данных на пересечении таблицв А с таблицей В.

RIGHT [ OUTER ] JOIN
Оператор правого внешнего соединения. 
Заголовок таблицы-это объединение заголовков соединяемых таблиц.
Оператор выполняет соединение левой и правой таблиц по условию.
Если дано две таблицы, A и B, захватывает всю правую часть (В), и часть данных на пересечении таблицы А с таблицей В.

FULL [ OUTER ] JOIN
Оператор полного внешнего соединения. 
Заголовок таблицы-это объединение заголовков соединяемых таблиц.
Результата работы оператора-выполняется соединение первой и второй таблиц по условию. 
Если дано две таблицы, A и B, захватывает таблицу А И В.

CROSS JOIN
Оператор перекрёстного соединения.
Заголовком таблицы будет объединение заголовков соединяемых таблиц.
Тело результата формируется следующим образом:
Каждая строка одной таблицы соединяется с каждой строкой второй таблицы, 
и дает в результате все возможные сочетания строк двух таблиц.
